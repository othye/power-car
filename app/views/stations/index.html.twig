{% extends "base.html.twig" %} 

{% block content %}
<section>
    <article>
        <form class="form" method="get" action="{{ url('search') }}" name="formulaire">
            <div class="input">
                <label for="zip"> Indiquez un CodePostal ou une Ville : </label>
                <div> <input name="search" id="search" type="search" placeholder="Ex : 39100 ou DOLE"></div>
            </div>
            <input class="btn" value="Recherche" name="envoi" id="envoi" type="submit">
        </form>
        <div id="map"></div>
    </article>
</section>
{% endblock %}


{% block javascripts %}
    {{ parent() }}
    <script>
        var search = {{ stations|json_encode()|raw }};
        $.each(search, function(key, element) {
            var ltd = element.latitude;
            var lgd = element.longitude;


            console.log(ltd);
            console.log(lgd);
            console.log(element.zip)
            

            $.each(element.technicals, function(key2, technical) {
                console.log(technical);
            });
        });
    </script>
{% endblock %}